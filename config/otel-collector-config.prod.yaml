receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
  docker_stats:
    endpoint: unix:///var/run/docker.sock
    collection_interval: 60s
  # Scrapes stub_status on the docker nginx (see nginx/default.conf, the
  # :8081/status server block). Reachable on the internal compose network.
  nginx/app:
    endpoint: "http://nginx:8081/status"
    collection_interval: 30s
  # Scrapes stub_status on the NixOS host nginx via the docker bridge
  # gateway. The host firewall + nginx-level allow rules keep this
  # endpoint private to docker bridges.
  nginx/host:
    endpoint: "http://host.docker.internal:8082/status"
    collection_interval: 30s

processors:
  batch:
    timeout: 1s
    send_batch_size: 1024
  resource:
    attributes:
      - action: insert
        key: service.instance.id
        value: "${HOSTNAME}"
      - action: insert
        key: deployment.environment.name
        value: "${OTEL_ENVIRONMENT}"
  # The nginx receiver emits resourceless metrics; stamp service.name so
  # the nginx-metrics dataset has a distinguishing column per scrape target.
  # Routing to the dataset is done via the x-honeycomb-dataset header on
  # the otlp_http/metrics-nginx exporter, not by service.name.
  resource/nginx-app:
    attributes:
      - action: insert
        key: service.name
        value: nginx-syllabus-tracker
  resource/nginx-host:
    attributes:
      - action: insert
        key: service.name
        value: nginx-do-host
  tail_sampling:
    decision_wait: 10s
    num_traces: 1
    policies:
      [
        {
          name: health-check-low-sampling,
          type: and,
          and:
            {
              and_sub_policy:
                [
                  {
                    name: health-check-endpoint-policy,
                    type: string_attribute,
                    string_attribute:
                      {
                        key: uri,
                        values: [.*\/health.*],
                        enabled_regex_matching: true,
                      },
                  },
                  {
                    name: sample-health-checks,
                    type: probabilistic,
                    probabilistic: { sampling_percentage: 2 },
                  },
                ],
            },
        },
        {
          name: fully-sample-default,
          type: and,
          and:
            {
              and_sub_policy:
                [
                  {
                    name: not-health-check-endpoint,
                    type: string_attribute,
                    string_attribute:
                      {
                        key: uri,
                        values: [.*\/health.*],
                        enabled_regex_matching: true,
                        invert_match: true,
                      },
                  },
                  { name: always-sample, type: always_sample },
                ],
            },
        },
      ]

exporters:
  # No x-honeycomb-dataset header: in a Honeycomb Environment, traces
  # land in the dataset named after service.name. So:
  #   service.name = syllabus-tracker        -> syllabus-tracker dataset
  #   service.name = nginx-syllabus-tracker  -> nginx-syllabus-tracker dataset
  #   service.name = nginx-do-host           -> nginx-do-host dataset
  otlp_http/traces:
    endpoint: "https://api.honeycomb.io:443"
    headers:
      "x-honeycomb-team": "${HONEYCOMB_API_KEY}"
  # Metrics need explicit dataset routing; unlike traces, Honeycomb does NOT
  # auto-route metrics by service.name. Both app metrics and nginx metrics
  # require an x-honeycomb-dataset header.
  otlp_http/metrics:
    endpoint: "https://api.honeycomb.io:443"
    headers:
      "x-honeycomb-team": "${HONEYCOMB_API_KEY}"
      "x-honeycomb-dataset": "syllabus-tracker-metrics"
  # Metrics do NOT route by service.name (only traces do); without an explicit
  # dataset header these land in `unknown_metrics`. Route both nginx scrapes
  # (app + host) to one `nginx-metrics` dataset; the service.name column
  # distinguishes nginx-syllabus-tracker from nginx-do-host.
  otlp_http/metrics-nginx:
    endpoint: "https://api.honeycomb.io:443"
    headers:
      "x-honeycomb-team": "${HONEYCOMB_API_KEY}"
      "x-honeycomb-dataset": "nginx-metrics"
  debug:
    verbosity: normal

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch, resource, tail_sampling]
      exporters: [otlp_http/traces]
    metrics:
      receivers: [docker_stats, otlp]
      processors: [batch, resource]
      exporters: [otlp_http/metrics]
    metrics/nginx-app:
      receivers: [nginx/app]
      processors: [batch, resource, resource/nginx-app]
      exporters: [otlp_http/metrics-nginx]
    metrics/nginx-host:
      receivers: [nginx/host]
      processors: [batch, resource, resource/nginx-host]
      exporters: [otlp_http/metrics-nginx]

  telemetry:
    metrics:
      level: none
