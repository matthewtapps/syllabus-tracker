- Multi-tenancy, integrated easy billing
- Add video uploads and viewing from the app
    - Can we store this while keeping on using SQLite? Or should we provision some external storage? the droplet doesn't have a lot of memory. Can we integrate with a google drive?
- Refactor database file into domain-segmented files
- After graduating a student, I got stuck on their page, needed to refresh for any of the buttons to work to go back to dashboard
- Graduated students shouldn't appear on coach/admin dashboards.
- The student technique non-expanded row is very busy; the icon button to go to the detail page falls onto the second row in practice in most cases. I think we can move the attempt button into the collapsible section.
- Did not get a prompt to install PWA on android, but it did work when I manually went to my browser settings and added it to my homescreen.
- PWA icon is the magic school bus picture instead of the gym logo, should just be the gym logo.
- Migrate from DO droplet to hetzner
- It looks like 401s don't send us back to the login screen like they should, I end up stuck on screens saying things like "Could not fetch techniques", should automatically deauth and send to login screen
