- Multi-tenancy, integrated easy billing
- Refactor database file into domain-segmented files
- Migrate from DO droplet to hetzner
- When clicking the tabs in the "Add technique" modal, the height/content of the modal shifts dramatically and is a bit rough on the UX. If we can pin the top of the modal to a specific height on the screen, and thereby keep the tabs that we're selecting in the same position and just modify the content BELOW the tabs when changing, I think it will feel sleeker.
- Admin view could use tabs for graduated/archived/active/all similar to student list. Also put display tags on the user rows too for 'graduated' 'archived', like the student list. Default selection should be "active".
- From student list, if I've selected a tab like "graduated", then selected a student to go to their technique list, then press "back to student list", it currently takes me back to the "Active" tab. It should take me back to the tab that I was on.

